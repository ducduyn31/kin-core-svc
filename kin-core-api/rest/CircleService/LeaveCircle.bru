meta {
  name: LeaveCircle
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/v1/circles/:circle_id/leave
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}
