meta {
  name: AddMember
  type: http
  seq: 8
}

post {
  url: {{base_url}}/api/v1/circles/:circle_id/members
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}

body:json {
  {
    "user_id": "user-uuid-here",
    "role": "MEMBER_ROLE_MEMBER"
  }
}
