meta {
  name: GetCircle
  type: http
  seq: 3
}

get {
  url: {{base_url}}/api/v1/circles/:circle_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}
