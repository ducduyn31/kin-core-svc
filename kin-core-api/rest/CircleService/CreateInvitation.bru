meta {
  name: CreateInvitation
  type: http
  seq: 12
}

post {
  url: {{base_url}}/api/v1/circles/:circle_id/invitations
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}

body:json {
  {
    "type": "INVITATION_TYPE_LINK",
    "max_uses": 10,
    "expires_in_hours": 72
  }
}
