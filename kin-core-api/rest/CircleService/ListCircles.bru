meta {
  name: ListCircles
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/circles
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:query {
  ~limit: 10
  ~offset: 0
}
