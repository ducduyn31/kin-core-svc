meta {
  name: DeleteCircle
  type: http
  seq: 5
}

delete {
  url: {{base_url}}/api/v1/circles/:circle_id
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}
