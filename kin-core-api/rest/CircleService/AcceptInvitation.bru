meta {
  name: AcceptInvitation
  type: http
  seq: 13
}

post {
  url: {{base_url}}/api/v1/circles/join
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "code": "invitation-code-here"
  }
}
