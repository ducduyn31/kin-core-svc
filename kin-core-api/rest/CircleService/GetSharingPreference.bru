meta {
  name: GetSharingPreference
  type: http
  seq: 10
}

get {
  url: {{base_url}}/api/v1/circles/:circle_id/sharing
  body: none
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

params:path {
  circle_id: {{circle_id}}
}
