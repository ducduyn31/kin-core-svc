meta {
  name: UpdateTimezone
  type: http
  seq: 3
}

post {
  url: {{base_url}}/kin.v1.UserService/UpdateTimezone
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "timezone": "America/New_York"
  }
}
